apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: portfolio-cert
  namespace: default
spec:
  secretName: porfolio-cert-tls
  dnsNames:
    - prathamesh.secyour.space
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  duration: 2160h # 90 days
  renewBefore: 360h # 15 days before expiry
